class Cow {
    /**
     * Creates a cow with name `name`.
     * 
     * @param {string} name 
     */
    constructor(name) {
        this.name = name;
    }

    /**
     * Returns the sound of a cow.
     * 
     * @return {string} Sound
     */
    talk() {
        return "Muh";
    }
}
